#include <iostream>
#include<limits>
using namespace std;
int maxSum(int* arr, int n)
{
	int current = 0;
	int prevMax = INT_MIN;
	for(int i = 0;i < n;i++)
	{
		if(arr[i] )
	}
}

int main()
{
	int arr[MAX];
	int n;
	cout << "Enter the number of elements: ";
	cin >> n;
	for(int i =0;i < n;i++)
		cin >> arr[i];


	cout << maxSum(arr, n);
}